import { Replacer } from "./replacer";

/**
 * Replaces note names in tabs.
 */
export class TabsReplacer extends Replacer {
    register(): void {
        // TODO
    }

    unregister(): void {
        // TODO
    }
}
